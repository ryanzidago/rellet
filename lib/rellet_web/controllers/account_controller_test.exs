defmodule RelletWeb.AccountControllerTest do
  use RelletWeb.ConnCase

  # alias Rellet.RandIDGen

  # setup do
  #   account_id = RandIDGen.gen_account_id()

  #   {
  #     :ok,
  #     account_id: account_id
  #   }
  # end

  # describe "index/2" do
  #   test "returns a json response with all accounts of a user", %{conn: conn} do
  #     assert conn = get(conn, Routes.account_path(conn, :index))
  #     assert response = json_response(conn, 200)

  #   end
  # end

  # describe "show/2" do
  #   test "returns a json response with a given account for an account_id", %{
  #     conn: conn,
  #     account_id: account_id
  #   } do
  #     assert conn = get(conn, Routes.account_path(conn, :show, account_id))
  #     assert response = json_response(conn, 200)

  #   end
  # end
end
